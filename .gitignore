# IDE
.idea/

# Python caches
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Large extracted OSHA dataset (regenerated by unzipping context_risk/cleaned_data.zip).
# The small hazard-frequency CSVs the simulator consumes live in data/ and ARE kept.
training/context_risk/cleaned_data/osha_construction_vehicles_cleaned.csv

# Local runtime artifacts
data/events.db

# Training artifacts (TensorBoard logs are large; the saved PPO policy IS kept)
training/pipeline_b_simulator/runs/
training/**/*.tfevents.*

# Downloaded models
face_landmarker.task

# Face ID — key material, biometric templates and supervisor PIN hashes.
# None of this may ever be committed: templates are personal data under the
# DPDP Act, and the key file is what protects them.
data/faceid.key
data/templates.enc
data/supervisors.json
arcface_r50.onnx
